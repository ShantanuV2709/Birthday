{
  "name": "birthday-monorepo",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build && cd .. && rm -rf public && mkdir public && cp -r client/dist/* public/"
  }
}




